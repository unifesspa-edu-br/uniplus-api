replicaCount: 2

image:
  repository: registry.unifesspa.edu.br/uniplus/portal-api
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    cpu: "1"
    memory: 512Mi

env:
  ASPNETCORE_ENVIRONMENT: Production
  ASPNETCORE_URLS: http://+:8080

secrets:
  connectionString: ""
  redisConnection: ""
  kafkaBootstrapServers: ""
  storageEndpoint: ""
  storageAccessKey: ""
  storageSecretKey: ""
  authAuthority: ""

healthcheck:
  path: /health
  initialDelaySeconds: 15
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 6
  targetCPUUtilizationPercentage: 70
