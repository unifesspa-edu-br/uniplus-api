services:
  selecao-api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.selecao
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__SelecaoDb: "Host=postgres;Port=5432;Database=uniplus_selecao;Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}"
      Redis__ConnectionString: "redis:6379"
      Kafka__BootstrapServers: "kafka:29092"
      # Schema Registry (Apicurio em modo Confluent-compat) — sufixo /apis/ccompat/v7
      # é mandatório (Apicurio 3.x). Auth desligada em dev (AuthType=None default).
      SchemaRegistry__Url: "http://apicurio:8081/apis/ccompat/v7"
      Storage__Endpoint: "minio:9000"
      Storage__AccessKey: "${MINIO_ROOT_USER}"
      Storage__SecretKey: "${MINIO_ROOT_PASSWORD}"
      Auth__Authority: "http://keycloak:8080/realms/unifesspa"
    ports:
      - "5202:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
      apicurio:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  ingresso-api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.ingresso
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__IngressoDb: "Host=postgres;Port=5432;Database=uniplus_ingresso;Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}"
      Redis__ConnectionString: "redis:6379"
      Kafka__BootstrapServers: "kafka:29092"
      # Schema Registry — Ingresso é consumer cross-módulo de eventos Selecao;
      # SR Url permite recuperar schemas dos eventos publicados.
      SchemaRegistry__Url: "http://apicurio:8081/apis/ccompat/v7"
      Storage__Endpoint: "minio:9000"
      Storage__AccessKey: "${MINIO_ROOT_USER}"
      Storage__SecretKey: "${MINIO_ROOT_PASSWORD}"
      Auth__Authority: "http://keycloak:8080/realms/unifesspa"
    ports:
      - "5262:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
      apicurio:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  portal-api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.portal
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__PortalDb: "Host=postgres;Port=5432;Database=uniplus_portal;Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}"
      Redis__ConnectionString: "redis:6379"
      Kafka__BootstrapServers: "kafka:29092"
      Storage__Endpoint: "minio:9000"
      Storage__AccessKey: "${MINIO_ROOT_USER}"
      Storage__SecretKey: "${MINIO_ROOT_PASSWORD}"
      Auth__Authority: "http://keycloak:8080/realms/unifesspa"
    ports:
      - "5302:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  organizacao-api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.organizacao
    restart: unless-stopped
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__OrganizacaoDb: "Host=postgres;Port=5432;Database=uniplus_organizacao;Username=uniplus_organizacao_app;Password=${POSTGRES_PASSWORD}"
      Redis__ConnectionString: "redis:6379"
      Kafka__BootstrapServers: "kafka:29092"
      Storage__Endpoint: "minio:9000"
      Storage__AccessKey: "${MINIO_ROOT_USER}"
      Storage__SecretKey: "${MINIO_ROOT_PASSWORD}"
      Auth__Authority: "http://keycloak:8080/realms/unifesspa"
    ports:
      - "5263:8080"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
