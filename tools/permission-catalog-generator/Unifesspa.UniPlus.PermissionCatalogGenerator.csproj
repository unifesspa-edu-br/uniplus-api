<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>

  <ItemGroup>
    <!-- Parser YAML real para a fonte declarativa — regex/parser artesanal não cobre a
         gramática do formato de forma confiável (aspas, escapes, etc.). -->
    <PackageReference Include="YamlDotNet" />
  </ItemGroup>

</Project>
