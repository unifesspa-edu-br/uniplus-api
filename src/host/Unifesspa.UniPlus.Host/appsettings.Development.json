{
  "//": "Monólito modular (spike): processo único, banco único `uniplus` com schema-por-módulo. As 5 connection strings apontam para o MESMO banco — cada DbContext usa seu schema (HasDefaultSchema); UniPlusDb é usada pelo outbox Wolverine (schema `wolverine`) e pelos health checks. Em dev usa o superusuário; o init-db variante (4 schemas + role-por-schema) é a forma de rollout.",
  "ConnectionStrings": {
    "UniPlusDb": "Host=localhost;Port=5432;Database=uniplus;Username=uniplus;Password=uniplus_dev",
    "ConfiguracaoDb": "Host=localhost;Port=5432;Database=uniplus;Username=uniplus;Password=uniplus_dev",
    "OrganizacaoDb": "Host=localhost;Port=5432;Database=uniplus;Username=uniplus;Password=uniplus_dev",
    "SelecaoDb": "Host=localhost;Port=5432;Database=uniplus;Username=uniplus;Password=uniplus_dev",
    "IngressoDb": "Host=localhost;Port=5432;Database=uniplus;Username=uniplus;Password=uniplus_dev"
  },
  "Redis": {
    "ConnectionString": "localhost:6379"
  },
  "Kafka": {
    "BootstrapServers": "localhost:9092"
  },
  "Storage": {
    "Endpoint": "localhost:9000"
  },
  "Auth": {
    "Authority": "http://localhost:8080/realms/unifesspa",
    "Audience": "uniplus"
  },
  "UniPlus": {
    "Encryption": {
      "//": "DEV ONLY — produção usa Provider=vault via env vars.",
      "Provider": "local",
      "LocalKey": "nnbUeo/I+i8nGIaa3cOxPZ4j6IUUlMAiMBAZXS00Rec="
    }
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:4200",
      "http://localhost:4201",
      "http://localhost:4202",
      "http://localhost:4203"
    ]
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft": "Information",
        "Microsoft.EntityFrameworkCore.Database.Command": "Information"
      }
    }
  }
}
